V1.3 Worker router additions

import { handleUpload } from "./upload.js";
import { handleDelivery } from "./delivery.js";

if (url.pathname.startsWith("/upload/")) return handleUpload(request, env, url);
if (url.pathname === "/delivery") return handleDelivery(request, env, url);

Bindings/secrets:
- R2_ACCOUNT_ID
- R2_ACCESS_KEY_ID
- R2_SECRET_ACCESS_KEY
- R2_BUCKET=socaln-media
- R2 binding MEDIA_BUCKET -> socaln-media
- KV binding INTAKES
- ALLOWED_ORIGIN=https://socalnmotorsports.store

R2 CORS must expose `ETag` for multipart uploads.
